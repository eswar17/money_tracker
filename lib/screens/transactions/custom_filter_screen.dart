import 'package:flutter/material.dart';

class CustomFilterScreen
    extends StatelessWidget {

  const CustomFilterScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {

    return const Scaffold(

      body: Center(

        child: Text(
          'Custom Filter Screen',
        ),
      ),
    );
  }
}