import 'package:flutter/material.dart';
class AppShadows {
  static final card = [
    BoxShadow(
      color: Colors.black.withOpacity(0.04),
      blurRadius: 18,
      offset: const Offset(0, 8),
    ),
  ];
}